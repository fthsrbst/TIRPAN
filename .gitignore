# Environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Database
*.db
*.db-shm
*.db-wal
*.sqlite

# Sensitive runtime data — never commit
data/.credkey
data/sessions/
data/training/
data/playbook.jsonl
data/branding/

# Build artifacts
site/
# NOTE: web/static/normal/ (the built "normal" React UI) is intentionally
# committed so fresh clones serve /normal/ without a Node toolchain.
# Rebuild with: cd attack-graph-canvas && npm install && npm run build

# Reports output
reports/

# Logs
*.log

# Coverage
.coverage
htmlcov/

# IDE
.vscode/
.idea/

# Claude / AI dev tools (local config, personal paths)
.claude/

# OS
.DS_Store
Thumbs.db

# ML models (trained artifacts — large binary files)
ml/models/
ml/data/
